<template>
    <div class="bg-[var(--bg-primary)] rounded-token-lg border border-[var(--border-primary)] p-6">
      <h2 class="text-lg-semibold text-[var(--text-primary)] mb-4">Copy Referral Link</h2>
      <div class="flex items-center">
        <input type="text" class="flex-1 border border-[var(--border-primary)] rounded-l-md p-2" readonly />
        <button class="bg-[var(--bg-secondary)] text-[var(--text-primary)] rounded-r-md px-4">
          Copy
        </button>
      </div>
    </div>
</template>